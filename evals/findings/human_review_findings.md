# Human Review Findings

This document summarizes exported human review outcomes from SQLite.

- Total reviewed items: **83**
- Overrides (`override_pass`): **50**
- Approved fails (`approve_fail`): **28**

## Breakdown by Check and Human Label

- `entity_resolution` / `override_pass`: **14**
- `entity_resolution` / `approve_fail`: **10**
- `semantic_accuracy` / `approve_fail`: **18**
- `semantic_accuracy` / `override_pass`: **9**
- `semantic_accuracy` / `needs_changes`: **5**
- `source_credibility` / `override_pass`: **27**

## Notes
- Use these artifacts for governance and prompt tuning decisions.
- Compare this with drift reports to understand version impact.